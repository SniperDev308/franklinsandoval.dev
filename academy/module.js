const cfg=window.FTA_MODULE||{};const KEY=cfg.key||"fta-module";let state={lab:false,incident:"",quiz:false,score:0,complete:false};try{state={...state,...JSON.parse(localStorage.getItem(KEY)||"{}")}}catch{}const lab=document.getElementById("labDone"),inc=document.getElementById("incidentAnswer"),grade=document.getElementById("gradeBtn"),result=document.getElementById("quizResult"),complete=document.getElementById("completeBtn");lab.checked=state.lab;inc.value=state.incident||"";function save(){state.lab=lab.checked;state.incident=inc.value;localStorage.setItem(KEY,JSON.stringify(state));refresh()}function refresh(){complete.disabled=!(state.lab&&state.quiz&&state.score>=67);if(state.quiz)result.textContent="Knowledge check: "+state.score+"%"+(state.score>=67?" · PASS":" · REVIEW AND RETRY");if(state.complete){document.getElementById("moduleStatus").textContent="Complete";document.getElementById("moduleHint").textContent="Module evidence recorded on this device.";complete.textContent="Module Completed ✓";complete.disabled=true}}lab.addEventListener("change",save);inc.addEventListener("input",save);grade.addEventListener("click",()=>{const answers=cfg.answers||{};let n=0,total=Object.keys(answers).length;for(const [name,val] of Object.entries(answers)){if(document.querySelector('input[name="'+name+'"]:checked')?.value===val)n++}state.score=Math.round(n/total*100);state.quiz=true;save()});complete.addEventListener("click",()=>{state.complete=true;localStorage.setItem(KEY,JSON.stringify(state));if(cfg.completeKey)localStorage.setItem(cfg.completeKey,"true");if(cfg.progress)localStorage.setItem("fta-program-progress",cfg.progress);refresh()});refresh();