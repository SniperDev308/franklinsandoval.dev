document.addEventListener("DOMContentLoaded",()=>{let complete=false;try{complete=localStorage.getItem("fta-day0-complete")==="true"}catch{}const empty=document.getElementById("empty"),results=document.getElementById("results");if(!complete){empty.hidden=false;results.hidden=true;return}const r=saveScore();document.getElementById("overall").textContent=r.overall+"%";const entries=Object.entries(r.domains);document.getElementById("matrix").innerHTML=entries.map(([k,d])=>'<div class="matrix-row"><b>'+d.label+'</b><div class="bar"><i style="width:'+d.score+'%"></i></div><strong>'+d.score+'%</strong></div>').join("");const sorted=[...entries].sort((a,b)=>a[1].score-b[1].score);document.getElementById("priority").innerHTML=sorted.map(([k,d],i)=>'<div class="priority-item"><b>0'+(i+1)+'</b><span>'+d.label+'</span><small>'+d.score+'%</small></div>').join("");});